# SURFACE — COMPATIBILITY ALIAS

This path is retained because the current `ADMISSION.json.local_session_environment` activation graph still references it.

It contains **no independent policy**. The single current normative local cognitive policy is [`LOCAL_SESSION_ENVIRONMENT.md`](LOCAL_SESSION_ENVIRONMENT.md), profile `JURISCRIBE_LOCAL_COGNITIVE_RUNTIME_V1`.

When this alias is activated, apply the root cognitive policy from the same pinned revision. Juriscribe canonical contract/runtime remains authoritative; this alias adds no authority node.
